import * as React from 'react'
import { UnknownItem } from '../../shared/model/item'

const HiddenItem = (
    props: {
        item: UnknownItem
    }
) => {


    return (
        <div>
            ?
        </div>
    )
}

export default HiddenItem
