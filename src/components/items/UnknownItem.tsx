import * as React from 'react'
import { UnknownItem } from '../../shared/model/item'

const UnknownItem = (
    props: {
        item: UnknownItem
    }
) => {


    return (
        <div>
            ?
        </div>
    )
}

export default UnknownItem
